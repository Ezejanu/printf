#ifndef MAIN_H
#define MAIN_H

#include <string.h>
#include <math.h>
#include <unistd.h>
#include <stdarg.h>

int _printf(const char *format, ...);
int add(int a, int b);
int _putchar(char c);
int _puts(char *s);
int getint(int numb);

#endif
